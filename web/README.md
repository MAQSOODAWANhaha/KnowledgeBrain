# 投标台

Apple / iCloud 工作台。打开标是一张台：商务 / 勾选段 / 成稿，主列 [表 | 文稿]。

```bash
cd web && npm install && npm run dev
# http://127.0.0.1:5174  代理到 :8080

npm run build
export KNOWLEDGEBRAIN_WEB_ROOT=/opt/workspace/code/KnowledgeBrain/web/dist
```
