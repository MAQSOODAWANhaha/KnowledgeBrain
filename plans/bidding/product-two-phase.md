# 招标解析与投标文件生成：统一设计与实施任务

更新时间：2026-09-18。

本文是招投标生成流程的**唯一现行设计和任务账本**。2026-09-18 的要求归类、组织效率和核对闭环修订见第 11 节，状态为**待开始**；第 3–5 节已同步目标合同，不代表这些修订已经部署。实现与验收状态见第 9 节。方案存在、代码已写或局部单测通过，都不代表产品验收完成。

## 1. 目标与业务边界

唯一用户流程：

```text
上传招标文件及补遗 → 解析完成 → 用户点击“生成章节大纲” → 冻结本次输入
→ 自动生成完整章节大纲和骨架 Word
→ 用户可选 AI 补充内容、自由编辑 → 保存、下载当前投标文件
```

大纲任务内部依次执行：**发现 → 组织 → 核对 → 发布**。这是一个现有 Worker 任务中的四个阶段，不是四个服务、四个 Agent 或四次用户请求。发现过程中可以暂存候选组织建议，但不能提前发布。

约束：

- 不区分草稿和终稿。取消 `official / draft-fill` 双模式及其业务门禁；单值 `mode` 不保留。
- 大纲生成和可选内容补充是两次独立请求；上传后不能自动填完整正文。
- 当前已保存 DOCX 是用户正文主源，ONLYOFFICE 承担编辑和保存；PDF 必须由同一已保存版本转换。
- 企业资质、报价、人员、证书、证明和实际响应未知时保留待填位置，不能由招标要求推断成投标方事实。
- 常见约百页文档以 5–10 分钟为优化目标。**完整性优先，超过 15 分钟仍继续正常处理**；不靠截取前几个窗口满足耗时指标。
- 首次大纲只有核对通过才发布；本期不新增“部分大纲发布”分支。失败保留进度和问题，已有保存文件仍可编辑、下载。
- 用户编辑后的文件含无法可靠保留的图片、签章、域或排版时，阻止有损填充，保留原稿；本期不建设完整 DOCX 定点插入系统。
- **不兼容。** 不读取旧字段、旧枚举、旧工具参数、旧 API、旧检查点，也不做别名、默认填充、双写或升级转换。缺字段或未知字段直接拒绝。`official` / `draft-fill`、字符串版 `issues`、用 `Omitted` 混装失败原因，都不是新合同。旧运行不能恢复。这不授权清空数据库或删除用户已经保存的文件。

“完整”表示冻结范围已检查、所有识别出的适用提交要求有去向、引用有明确处理结论且发布阻断清单为空；未解但不阻断的问题须保留在文件与界面。语义核对仍可能犯错，真实文件必须以人工标注清单验收，不能承诺数学意义上的绝对无遗漏。

## 2. 架构与职责

| 组件 | 职责 | 边界 |
| --- | --- | --- |
| DocReader | 正文、表格、位置、原图、解析问题及回读兼容性 | 不决定章节完整性 |
| 宿主 | 输入校验、分块、投递、阶段推进、工具校验、预算、发布 | 不用关键词切分结果发明必备章节 |
| 模型 | 识别提交要求、组织章节、语义查漏、空章模板内容 | 不改任务预算、数据库版本，不调用发布 |
| Checkpoint | 保存业务进度、要求、章节、问题和核对状态 | 是业务事实来源，不能依赖聊天历史恢复大纲 |
| Journal／Rig session | 请求原字节预约、响应保存、执行提交与重放 | 执行恢复不代替业务完整性 |
| Word 编译与版本层 | 同一章节树编译、对象暂存、版本登记和当前身份更新 | 不判断要求是否适用 |
| 前端 | 操作、进度、问题、编辑和下载 | 不另算一套完成状态 |

复用现有 Worker、Rig、Journal、Checkpoint、对象存储、租约、版本 CAS、DocReader、Word 编译器、ONLYOFFICE 和转换能力。不新增 Agent 服务、队列或平行状态机，也不新增领域表保存重复业务库存。

删除旧正式编制 Agent、仅服务旧流程的提示词／工具／配置／路由／调度／验收门。共享原文读取、模板、编译、权限、存储和版本能力保留。删除以实际调用关系为准，不能按目录整块误删共用能力。

## 3. 四阶段执行

### 3.1 发现：完整检查冻结输入

**输入合同。** 冻结用户选择的文件及解析版本、摘要、角色、补遗／替代关系、处理决定、来源和解析问题。文件未解析完成、缺页、失败来源或只有未读图像的页面不能静默跳过。明确补遗才覆盖旧要求；不确定冲突保留两侧依据并形成问题。

**分块。** 全部有效正文和表格进入确定性分块计划；按文件、原始顺序组织，关键词和标题仅调整优先级。取消 8 窗上限及索引截半。单个长来源按 UTF-8 范围续读；长表按行／单元格续读并携带表头；跨页条款携带相邻上下文，重叠不重复记账。每批容纳多个来源，依实际请求字节、上下文与输出额度组包，不一页一回合。

`structured_forms` 只是解析出的表格，不等于指定投标格式。表格用途依原文、引用和内容判断；`source_unit:<UUID>` 不能当业务表名。无 `heading_path` 的 PDF 使用同一条路径。

**发现结果。** 每批明确提交已检查范围、提交要求、跨章节／跨文件引用及问题。允许“本批无提交要求”，但必须有范围处理结果。索引遍历、搜索命中、部分原文、失败工具、被截断输出不能作为整批完成凭据。

提交清单覆盖组成、分册、顺序、指定格式、资格、商务、报价、证明材料、明确技术响应、方案和评分支撑。普通合同叙述、投诉流程、附件编号不自动成为章节。参数逐项响应通常映射响应表，不逐参数建章。

每条发现条目保存稳定 ID、描述、处理类型、适用条件、精确依据、是否指定格式及格式依据。处理类型固定为提交物、内容约束、结构／文档约束、非文档事项，具体字段见第 11 节。适用条件与处理类型正交：上传规则不是“不适用材料”，评分权重不是提交物。要求记录可以保留多个来源版本，同一交付物优先共用章节；不按标题字符串自动合并。只有有依据的不适用提交项才从提交覆盖分母中排除，排除结论仍必须进入核对。

**阶段出口。** 全部有效范围完成检查、文件元数据已处理，跨文件引用均已定位或明确登记为 unresolved 并关联影响要求。发现完成不要求所有提示解决；它要求扫描完整、每条引用有去向而非静默丢失。未解引用是否阻断只由第 3.4 节判定。不以缺口数量两轮未下降中止正常扫描。

### 3.2 组织：要求映射为章节

- 先归类并核对适用性，再按交付物组织；这属于现有组织阶段，不增加第五阶段。提交物／内容约束与章节建立多对多映射，结构／文档约束进入对应核对范围，非文档事项保留在分析结果中。禁止每条原文要求机械建章，也禁止以标题包含匹配代替覆盖关系。
- 要求的 `grounds`、`format_grounds` 是权威依据。模型组织章节只提交结构和 requirement_ids；宿主按映射派生并保存章节 grounds／format_refs，组织节点还继承子树依据。不再要求模型复制已保存的 UUID 和字节范围。章节成立依据与正文格式依据分开，具体派生和修正规则见第 11 节。
- 标题可合理归纳，字符串绑定失败不能删除必需章节。格式未找到则保留章节、登记问题。
- 原文明确的分册、层级与顺序优先；未规定时按内容归属组织，不强制三级、不限制只有三级。
- 要求技术文件跟随某卷结构时，实际追踪对应原文并展开；不能只生成空“技术文件”。条款编号不是章节编号，不能强制两个编号体系一致。
- 区分组织性节点与正文承载节点。组织节点不填正文；承载响应要求的父节点即使有子章也不能被“只填叶子”规则跳过。
- 批量变更按最终树原子校验：稳定 ID、父节点存在、同级顺序、无自引用、无祖先环、无孤立节点。新节点必须使用批内 tmp- ID；未知 chapter- ID 不能被当作新节点。删除或合并后要求仍须有去向。标点、顿号和括号不作为章节语义拆分的硬门；语义核对检查是否错误合并了原文要求独立提交的材料。
- 不适用、尚未完成、格式缺失三者分开。不得继续用 `Omitted` 混装解析失败、绑定失败、超时和不适用。
- 预览和 Word 共用规范化章节树及过滤规则，不能分别提升子节点或删除父标题。

### 3.3 核对：既查映射，也查清单本身

宿主确定性检查严格使用第 3.4 节唯一阻断清单，不增加未定义的“关键问题”门槛。

**核对输入边界固定。** 仅输入已保存要求、大纲，以及宿主从发现阶段保存的依据中抽出的组成、指定格式、资格、技术结构／响应、报价、评分和补遗片段。扫描时即将这些条款保存为 `review_fragments`，包括判断没有生成要求的相关条款；不能只从已生成章节倒推片段，否则漏提项没有机会被查出。按精确范围去重，保留完整条款及必要表头、跨页续句，不以摘要代替原文。

顶层组成先单独核对一次，输入所有组成／顺序片段和各分册摘要。随后按分册组织核对包，输入该册要求、子树及关联片段。跨册条款作为共享片段引用，顶层不复制整份子树；修改顶层归属／顺序才使顶层核对失效，册内修改只重核该册及受影响共享关系。

所有发现条目的分类与适用性结论都必须有核对归属，包括没有章节的非文档事项和不适用材料；不能只从已建章节反推验收分母。核对包摘要只包含该包负责的要求、章节、引用和片段；以变更前后的实际内容摘要决定是否重查，不因无关修改或无操作提交全量失效。

核对工具只允许 `read_outline`、读取当前核对包内片段的分页／续段及提交核对结论；宿主检查范围，拒绝包外读取、全书索引、自由搜索和重新发现式翻页。发现包外疑点时提交带原依据和目标描述的问题，由宿主返回发现阶段执行定向查找，禁止从第一窗重扫。新证据补入片段并使受影响核对摘要失效。材料无法取得时登记 unresolved，按唯一发布门处理，不能无限检索。

**有效进展固定。** 每个问题保存最近两轮的语义签名：关联片段内容摘要、要求的内容／适用性／依据、关联章节内容／映射／层级／顺序、引用状态／目标／处置依据。排除时间戳、ID 重排、措辞化的修补笔记和缺口总数。宿主只接受与该问题有关且有来源支撑的字段变化；同一签名重复两轮或出现 A→B→A 循环均视为无进展。阻断问题无进展则任务失败并保留检查点；非阻断提示无进展则保留提示并结束该问题修补，不阻止其他核对和发布。核对结论必须匹配当前片段和大纲摘要。

该核对是生成内部阶段，不恢复逐页 Reviewer。一次初核加有进展的定向修补，不重新通读全文，不因提示未清零继续空转。

### 3.4 发布：核对通过且文件提交成功

**唯一业务发布门：核对包均有当前版本结论，且宿主派生的 `blockers` 为空。** 模型不得自行把 blocker 降级为提示，`issues` 非空不等于禁止发布。

| 阻断码 | 唯一判定条件 |
| --- | --- |
| B_SCAN_INCOMPLETE | 冻结有效正文／表格／元数据未检查完，或已知缺页、失败解析导致该范围无法检查 |
| B_REQUIREMENT_UNMAPPED | 适用／条件性的提交物或内容约束无有效承接；结构／文档约束或排除结论没有核对归属；未解引用决定必备子项或规定层级且无法枚举时也属未覆盖，不能用笼统父标题冒充已映射 |
| B_TREE_INVALID | 环、孤儿、无依据章节、非法映射／顺序，或预览与 Word 不是同一树 |
| B_CONFLICT_FALSELY_RESOLVED | 冲突被标为已解决／不适用却没有依据，或编译偷偷选择某一侧；无法同时保留必要结构的冲突按 B_REQUIREMENT_UNMAPPED 处理 |
| B_FORMAT_EVIDENCE_MISSING | format_required 为真的适用要求没有有效原文／网格格式依据，或保存依据失效、丢失；不能依赖自由字符串 kind 恰好等于 format |

例子：引用外部标准只影响后续填写且不改变章节／格式时，保留 unresolved 提示，可以发布；引用“附件列明的全部资格材料”而附件缺失、无法列出必备子件时，B_REQUIREMENT_UNMAPPED；指定报价表取不到原表时，B_FORMAT_EVIDENCE_MISSING。未确定的适用条件保留条件性章节。可将两侧结构完整保留的冲突按 unresolved 提示交付，不伪造裁决。

非阻断问题在界面显示，并在 Word 中相关章下放系统标记的“待确认说明”；文末集中列出文件／来源位置和问题。该说明是编译产物，不增加虚构投标章节；回读按系统标记识别，不能把仅有提示的章误判为已有用户正文。用户修改提示后按用户正文保护。提示清单绑定发布版本，禁止只存在日志里。

核对尚未执行／摘要失效时任务未到发布阶段；读取失败、版本 CAS、对象或数据库错误是技术发布失败，不能由问题降级机制绕过。不额外建立另一套语义阻断门。

顺序固定：固化结果及核对摘要 → 编译骨架 → 校验可读取和章节身份 → 对象暂存 → 数据库事务登记结果、版本、current 和成功状态 → 前端刷新。

首次生成绑定预期空版本；重新生成绑定提交时 current。执行期间用户保存新版本则 CAS 拒绝覆盖。对象暂存失败不标成功；数据库提交失败可清理暂存；已发布请求重放返回原结果，不再编译和登记第二版。

超性能目标继续；到保护预算，或阻断问题持续无进展时保持未完成，不能自动发布部分骨架。用户已有 current 继续可用。

## 4. 数据、工具与恢复合同

### 4.1 数据归属

| 数据 | 必需字段／语义 |
| --- | --- |
| 发现条目 | ID、描述、kind 四类、submission_name、classification_reason、required／conditional／not_applicable、条件、grounds、format_required、格式依据、顺序约束 |
| 待处理引用 | 引用依据、目标描述、解析目标、处理结论 |
| 章节 | ID、parent、order、title、组织／正文用途、requirement_ids、grounds、格式引用 |
| 正文状态 | 空、用户已有、已生成；与结构完整性分开 |
| 问题 | ID、类型、影响范围／节点、依据、解决状态、修补次数 |
| 核对 | 检查范围、输入／内容摘要、发现、通过状态 |
| 运行 | 阶段、分块游标、检查范围、累计调用／读取／花费、截止时间、发布身份 |

**T0-C 必须按下表固定 JSON 落点，T1／T2／T5 不得各建一份。** 以下是唯一合同，不表示当前代码已经按此序列化。全部进入现有检查点 JSON 与分析结果 JSON。反序列化使用 `deny_unknown_fields`，必填字段缺失即失败。不为旧形状提供 `#[serde(default)]`、别名或转换函数。SQL 只校验这份合同的摘要、身份与发布门，不另存一套业务状态，也不读旧列。

| 目标 JSON 路径 | 字段与拥有方 |
| --- | --- |
| `Checkpoint.analysis.outline.requirements[id]` | description、kind（submission／content_constraint／structure_constraint／non_document）、submission_name、classification_reason、applicability、condition、grounds、format_required、format_grounds、order_constraints；T2／O2 |
| `Checkpoint.analysis.outline.references[id]` | grounds、target_description、target_ids、requirement_ids、impact（structure／format／content）、status（resolved／unresolved）、resolution_grounds；T2 |
| `Checkpoint.analysis.outline.review_fragments[id]` | kind、原文／网格 Span、document_id、volume_ids；文本由冻结输入派生，不复制可变正文；T1 收集，T3 组包 |
| `Checkpoint.analysis.outline.issues[id]` | code、description（面向用户的问题说明）、requirement_ids、chapter_ids、reference_ids、grounds、status、resolution_grounds；T2；blocker 由宿主按 3.4 派生 |
| `Checkpoint.analysis.outline.checks[packet_id]` | scope、fragment_ids、snapshot_sha256、finding_ids、status；T3 |
| `Checkpoint.analysis.outline.scanned` | 正文／网格／元数据实际检查范围；T1；投递凭据仍仅在 `analysis.coverage` |
| `Checkpoint.analysis.draft_plan[]` | id、parent、order、title、purpose（group／response）、requirement_ids、grounds、format_refs、body_status；T2 定义，T5 消费；`draft_plan` 仅为现有存储字段名，不恢复草稿等级 |
| `Checkpoint.analysis.outline.id_sequences` | requirement／reference／issue／chapter 的分配序列；T0-C |
| `Checkpoint.outline_run` | phase、chunk_plan_sha256、chunk_cursor、active_check_packet、repair_signatures、no_progress_rounds；T3 |
| `Checkpoint.journal`、现有顶层 turn／tool_calls／read_bytes | 调用边界与累计额度，不另复制；T4 |
| `AnalysisResult.analysis` | 发布时保存上述 `Checkpoint.analysis` 的同一快照；报告和填充从此读取 |
| 填充请求现有 frozen JSON 的 `seed_plan_sha256`，填充 Checkpoint 的 `analysis.draft_plan` | 当前文档回读树与正文；T5 只复用章节合同，不另建 outline schema |
| 请求现有 frozen runtime JSON 的 `budget` | chunk_count、逻辑／物理／读取额度、total_timeout_secs、publish_reserve_secs；T4 |
| 现有 `bid_tender_agent_run_artifacts.started_at / hard_deadline_at` | 第一次领取确立 request_started_at／deadline，后续 attempt 复制同值；lease_acquired_at 仍为本次领取。返回给 Worker／Rust，不另建表；T4 |

稳定 ID 由宿主分配，删除后不复用，已提交响应重放不重复分配。`progress_detail`、blockers 和 UI 数量均只读派生，不能反向写入权威状态。T0-C 必须提供同一 JSON fixture 的 Rust round-trip、SQL 校验和前端 DTO 测试，完成后其他任务才能并行写字段。

### 4.2 三种范围状态

| 状态 | 何时写 | 用途 |
| --- | --- | --- |
| 已分配 | 宿主选择工作包 | 调度 |
| 已投递确认 | 原字节请求／工具结果实际确认 | 引用权限 |
| 已完成检查 | 有效扫描提交成功 | 发现完成门 |

复用已有 Coverage 表示投递凭据，新增扫描完成范围；分配窗口不预写全源已读。文本按实际字节范围，网格按实际读取语义，不能用稀疏 cells 数冒充全部网格。工具结果在下一次确认边界前不可被同一响应中的后续写入当成模型已见依据。

### 4.3 工具

| 工具 | 行为 |
| --- | --- |
| source_index／collection_index | 分页来源、文件角色、关系、处理决定；返回 next 和总量 |
| search_sources | 正文和表格有界搜索，位置、短上下文及分页；不登记检查完成 |
| read_source／read_form／read_source_view | 范围原文、网格、原图 |
| submit_outline_scan | 批量提交实际已检查范围、要求、引用和问题 |
| put_outline_items | 模型仅提交章节身份、父子、顺序、标题、用途、prescribed 和 requirement_ids；宿主派生依据，批量新增／修改／合并／删除按最终树原子校验 |
| read_outline | 分页恢复要求、章节、依据和问题 |
| submit_outline_check | 提交绑定范围和内容摘要的核对结论 |

宿主控制阶段出口，可提供显式组织完成操作；模型不能直接发布。批量父子节点支持批内临时引用，由宿主返回稳定 ID 映射。分页必须可继续，禁止只返回 truncated 却无法取尾部。读取允许跨窗口，但只能确认实际投递范围。

工具合同创建时冻结，按阶段限制可用操作，不在已预约请求上热改定义。删除旧标题关键词完整性工具和只有旧业务使用的工具。

### 4.4 Session／Journal

每轮从 Checkpoint 投影阶段、当前工作、相关要求、紧凑大纲、祖先与兄弟顺序、依据和问题。小树全量，大树顶层加当前分册，其余分页读取。历史消息可压缩，不能导致业务状态不可恢复。

扫描结果、章节更新、游标和阶段转换在既有提交边界一起保存。准备不推进业务，已预约恢复发原字节，已收到响应恢复不再次调用模型。清理核对上下文必须在完整工具批次处理后进行，不能在工具执行中破坏 assistant/tool 配对。

输入或合同摘要变化拒绝恢复。既有 pending／succeeded／failed 表示任务状态，阶段与未完成原因放进进度，不新增平行状态机。失败检查点保留用于诊断；不声称新合同可以恢复旧运行。

## 5. 预算、性能和异常出口

- 删除 8 窗、80 回合、20 分钟与固定两次尝试的领域捷径。
- `N` 定义为冻结输入经最终组包算法生成的**全部有效发现分块数**，包含长文本续段、网格续段和元数据，不是截断窗口数、来源条数或核对回合数。去重上下文不增加 N，首次创建即冻结分块计划摘要。
- 百页样本基线：已有 106 页／148 来源、124,776 正文字节、42 个表格，旧 8,000 字节按来源组窗离线计数为 20（未含完整网格序列化与元数据预算）。当前离线算法计入正文、网格、空来源及集合元数据后 N=204；废弃此前 20–60 的粗估。这是分块数量校准，不是模型耗时或完整性验收。T1 保留逐块字节、网格、范围及总 N，以实际冻结输入计算额度。
- 覆盖分块与模型阅读包分离：多个短来源在预算内合并投递；正文、网格和元数据仅按实际送达范围确认。游标指向首个未检查块，不要求每轮只检查一块；一次 `submit_outline_scan` 可提交多段结果。实际序列化预算仍由 evidence 投递层限制，分配来源不登记已读。 阅读包不再复用 8KB 常量：取工具结果上限、上下文字节上限的四分之一、预留输出 token 数的四倍的最小值（字节）。这是保守组包启发式，完整请求仍执行实际序列化和 token 估算校验，不宣称 token/字节精确换算。正文可跨记账块、表格可跨记账行片段，实际装不下时有界续读；扫描确认仍按实际区间累积。 完整请求包含历史、工具和状态后若超预算，发现阶段按当前包预算减半重建并重新校验完整请求，直至适配；禁止直接撤掉整包而静默退回自由补读。缩包是本地准备动作，不增加模型回合，也不修改冻结配置。实际包预算随已预留请求的 assigned_evidence.workload_bytes_limit 保存，收件和恢复按同一预算重建内容并校验相等，确认的范围只来自最终发送包。最小有效证据包也无法容纳时明确失败并保留检查点。
- `4N + 12` 仅为待校准初始逻辑保护额度，不能进入完整性判定。T1/T3 必须分别记录发现、补读、顶层核对、分册核对及修补的实测回合；不够则在新请求创建前调整冻结额度，不能运行中改合同或靠截断 N。废弃小输入帽 5／大输入帽 6 等旧断言，改验证全量范围与统一预算。
- 物理尝试、逻辑回合、读取量分别累计。**废弃**以 `L × (3t + w) + publish_reserve_secs` 推导整个任务截止的公式；本次 L=828、t=180秒导致472260秒（约5.47天），属于方案公式不合理，不是数据库未采用冻结值。总保护时间独立写入现有 runtime.budget.total_timeout_secs；单次调用、重试与退避只能使用其剩余时间，发布预留仍为300秒。首轮百页样本建议以60分钟作为待校准保护配置，5–10分钟仍是性能目标、15分钟是观察告警而非强制失败；大文件上线保护值须经代表样本校准，不能截断文件或靠扩大回合额度承诺成功。到保护时限保持未完成，不发布部分骨架。O6已将新请求冻结值改为3600秒，真实文件保护参数仍待校准，当前已运行请求不热改合同。
- 首次领取在现有 run 记录确定绝对 `hard_deadline_at`；后续领取复制首次 started_at 和 deadline，不重新加时。剩余不足 300 秒时不再开始新的模型尝试；已有有效核对结果的检查点仍可领取并在绝对截止前完成编译发布。模型阶段的 timeout 不包住编译阶段，SQL 不因进入预留区而拒绝全部重试。SQL claim／heartbeat、分析 heartbeat、Worker 读取同一截止时间。Worker 处理上限是 `max(0, deadline-now)`，外加现有 30 秒只用于清理的 margin。
- T4 已改领取函数、插入约束和 Worker 读取；数据库故障注入尚未在本环境执行，因此不能标已验证。
- 保留短租约、心跳、owner fencing；心跳不等于业务进展。单个任务重试不能延长首次截止或清零花费。
- 到帽、取消、超时、未处理响应或检查未通过，统一走未完成／失败出口；禁止 finish/异常兜底编译、omitted(bind_failed) 或“有节点就发布”。T3/T4 联合覆盖每个错误出口。
- 输出调用数、输入输出量、范围进度、阶段耗时和问题。实测校准完成前 T4 不得标记已验证；不将全时限相乘的故障保护值当成百页正常耗时。

## 6. 可选内容补充与文件保护

填充冻结 current 版本／摘要、原招标输入、大纲结果、回读树／正文摘要和本次模型合同。先确认保存完成，再回读；未保存内容不能进入请求。

以用户当前结构为准。书签优先定位身份，但保留书签不代表标题和依据未变化。改名／改变归属的章清空旧要求关联、格式来源和窗口，再重新定位；单章也必须重绑。失败保留空章和缺口，不能以旧依据填新标题。

已有正文不交给模型改写，只处理空的正文承载节点。没有可处理章时返回无操作结果，不登记无意义版本。

**本期可重建白名单写死：** 编译器标准样式的普通文本段落（含标准章节标题）、受支持网格结构的表格、可识别并重建的 TOC。段落中的混合样式、特殊对象或表格中的不支持属性不能因外层类型是段落／表格而放行。

图片、签章、扫描件、自定义样式、页眉页脚、首章前非 TOC 内容、无法完整回读正文或保留语义的 DATE／REF、其他未知对象，一律提交前拒绝。普通域的可见正文先完整回读，不能直接丢段；本期未通过域语义保真验证的 DATE／REF 仍拒绝。只允许重建的 TOC 例外，目录标题按已识别 TOC 载体处理，不把任意首章前内容当作目录丢弃。

不开发签章语义识别器，也不承诺“签章可重建”：图片／drawing、嵌入对象、数字签名包部件及未列入白名单的载体统一拒绝，覆盖签章和扫描件；检测目标是载体与格式，不是识别印章图案。用户以普通文本输入的名称按普通文本保留，不据此宣称识别或验证签名。

DocReader 检查整份包，包含关系部件、正文、页眉页脚与首章前内容，返回 part、unit_key／ordinal、可获得的章节／段落／表格位置和拒绝原因。无版面页码时返回包部件及顺序位置，不伪造页码。不能“先 continue 丢掉内容再报兼容”。现有图片拒绝只是部分实现，首章前内容和样式检查未完成前 T5 不得验收。

失败不创建填充任务或新版本，原稿继续可编辑下载。编译超过上限时失败保留原稿，禁止删用户正文降级出稿。

用户主动停止填充可发布已安全完成的正文，但保留全部章节和未填章，明确剩余原因。这不是部分大纲发布，因为结构已经完成。

填充发布按原 expected 版本 CAS；冲突不覆盖。成功由 FillPane 通知工作区刷新 current，编辑器和下载使用同一新版本身份。

API：填充统一 `/docx-fills`，含 basis、latest、任务查询和 stop；删除旧 `/docx-compositions` 业务入口及别名。大纲入口沿用现有请求身份并更新合同。

当前 DOCX 可直接下载；PDF 从同版 DOCX 转换。不要求旧正式编制／独立终稿复核。保留权限、已保存状态、文件有效性和转换检查。报告只记实际执行的检查、源版本及问题，不伪造未执行的语义批准。

## 7. 已发现问题的闭环映射

| 问题 | 设计落点 | 验收 |
| --- | --- | --- |
| 首窗闭合／扫描中两轮停滞即退出 | 发现完成前禁止组织完成，停滞只用于修补 | A01、A06 |
| 规则分母漏项，投诉／附件号／换行碎片误列 | 模型有据提交清单，语义核对清单本身 | A02、A04 |
| 表格全当指定格式 | 内容及引用判用途 | A05 |
| 8 窗／索引截断 | 完整分块与分页续读 | A03 |
| 分配冒充已读 | 三种范围分离 | A06 |
| grounds 丢失／按标题猜源 | 永久精确依据，格式另绑 | A07、A11 |
| session 压缩丢大纲 | Checkpoint 动态投影与分页恢复 | A09 |
| 父子环导致卡死 | 写入和发布树校验，遍历保护 | A10 |
| omitted 混淆，预览与 Word 不同 | 适用性和处理状态分离，同树渲染 | A11 |
| 重试重新计时 | 首次领取累计期限及调用账 | A12、A13 |
| 普通域正文丢失／改名沿用旧源 | TOC 特判、重绑清空、兼容性阻断 | A14、A15 |
| 图片样式损失／成功后下载旧版 | 提交前阻断有损填充、current 刷新 | A16–A18 |
| official 回归及双流程 | 删除旧入口和模式，不新增兼容补丁 | A20 |

## 8. 文档统一规则

- 本文维护设计、合同、任务和验收；`plans/bidding/README.md` 只做导航。
- `docs/bidding/prd.md` 维护当前产品行为；`authoring.md` 维护领域归属；`docx-composition.md` 维护编译／填充协议；`docx-rounds.md` 维护版本规则；不重复另一份业务方案。
- ONLYOFFICE 文档保留配置、保存、回调、转换、部署及实际验证事实，取消旧草稿／终稿流程依赖。
- 删除被替代的 agent-runtime-rig、outline-then-template、outline-expansion、remaining-implementation、agent-runtime-hardening、local-runtime-loop 方案；仍有效的技术约束并入本文及编辑器技术说明。
- 历史验证报告和 artifacts 保留为证据，明确不代表当前验收；不复制旧方案作为“历史兼容设计”。
- 全仓清理旧方案链接及失效锚点；平台和知识库文档只更新交叉引用，不改无关设计。
- 旧实现仍存在时写“待删除／待实施”，不能把新设计描述成已实现。

## 9. 实施任务与责任边界

当前按用户授权实施修复。代码已有局部修改，但尚未完成集成和产品验收；下表不得据此标记全部实现。

| 任务 | 负责内容 | 依赖 | 状态 | 验收出口 |
| --- | --- | --- | --- | --- |
| T0-D | 统一主文档、入口、产品／协议说明、旧文档删除与链接 | 无 | 已验证 | 本轮全仓当前 Markdown 链接、五档状态和工作树删除检查通过；未暂存／未提交，不代表代码验收 |
| T0-C | 共享 Rust／JSON／SQL／前端类型与错误码合同 | T0-D | 已实现待验证 | 同合同可序列化、校验、冻结；legacy 字段拒绝 |
| T1 | 完整分块、分页导航、搜索、实际投递与扫描范围 | T0-C | 已实现待验证 | A01、A03、A06：无 8 窗截断；chunk 计划按 UTF-8 记账且不重叠；长表续段携带表头但不重复记账。BiddingFile 离线校准：20 窗／204 块 |
| T2 | 提交清单、引用、精确依据、批量树、适用性和环校验 | T0-C | 已实现待验证 | A02、A07：无 heading_path 仍按正文绑定；归纳标题靠 requirement_id 保留。宿主不再用组成／格式词表发明必备章；未映射的已保存要求才阻断 |
| T3 | 四阶段调度、Session 状态投影、语义核对与修补 | T1、T2 | 已实现待验证 | A09：composition→volume 核对包；packet_snapshot；repair_signatures 同签／A→B→A；checked 需全部 packet pass |
| T4 | SQL、累计期限、幂等重放、租约、CAS 和对象发布 | T0-C；集成依赖 T3 | 已实现待验证 | A12、A13：首次领取冻结截止，重试复制同一值；数据库执行尚未在本环境跑过 |
| T5 | 回读、普通域、格式兼容性、重绑与填充保护 | T0-C、T2 | 已实现待验证 | A14–A16：页眉页脚／自定义样式／DATE 域拒绝；改名清空旧依据 |
| T6 | 真实进度、问题、预览、编辑器和版本刷新 | T0-C；集成依赖 T3–T5 | 已实现待验证 | A18：进度含 outline 字段；填充成功刷新 current |
| T7 | 当前文件下载／PDF、删除正式导出门禁 | T0-C、T4 | 已实现待验证 | A19：直接下载已保存 DOCX；PDF 同版转换；删除 draft 与独立复核发布硬门 |
| T8 | 废弃入口、模式、工具、提示词、配置、测试收敛 | T3–T7 | 已实现待验证 | A20：旧独立编制 Agent／调度／生产工具／schema／专属 SQL 与阶段值已删除；全目标编译、库及静态合同测试通过，真实数据库及链路验收归 T9 |
| T9 | 数据库故障、真实模型、真实文件和编辑器联合验收 | T1–T8 | 实现中 | A01–A20：本环境无 `KB_TENDER_AGENT_TEST_DATABASE_URL`／真实模型／ONLYOFFICE；A01／A02／T1 分块已用 BiddingFile 离线切片覆盖；其余联合链仍缺 |

本轮效率与正确性修订任务（以下状态按各自验收范围记录，不覆盖上表既有验收事实）：

| 任务 | 交付 | 依赖 | 状态 |
| --- | --- | --- | --- |
| O1 | 组织阶段补读权限、标题误拒、稳定ID及批量错误定位 | 无；先写真实失败回归 | 已验证 |
| O2 | 四类条目、指定格式标记、适用性核对、紧凑组织合同和宿主派生依据 | O1；先冻结共享JSON/schema | 已实现待验证 |
| O3 | 全条目核对归属、局部摘要、精确失效、引用与问题身份校验 | O2 | 实现中 |
| O4 | 按分册批量组织的状态投影、进展与阻断反馈、前端完成条件 | O2、O3 | 实现中 |
| O5 | 真实文件标注清单、原失败请求回放、模型端到端及完整性验收 | O1–O4；标注准备可提前 | 待开始 |
| O6 | 独立累计墙钟、阶段耗时／输出量校准、统一文档合同 | O2；上线参数依赖O5测量 | 实现中 |

分配约束：T0-C 先固定共享合同；T1、T2、T4、T5 可在合同后独立推进，但不同时写同一文件。SQL 基线由 T4 单一负责人维护，共享类型由合同负责人维护。隔离工作区可并行，主工作区单一集成人，不能覆盖已有未提交改动。删除旧链与新链交付在同一实施批次闭合，不留下兼容开关。

每项交付包含：变更文件、合同影响、测试命令与退出码、输出摘要、剩余问题、审查结论。状态只用待开始／实现中／已实现待验证／已验证／已验收，不以注释、mock 或旧版本测试替代本项验收。

## 10. 验收矩阵与证据

| 编号 | 场景 | 必须结果 |
| --- | --- | --- |
| A01 | 首窗商务，后文技术和报价 | 扫描完前不发布 |
| A02 | 无 heading_path 的真实 PDF | 技术第二卷、资格、价格正确映射 |
| A03 | 超 8 窗，尾部关键要求 | 最后一窗实际检查并进入结果 |
| A04 | 投诉、括号、换行、附件号 | 无错误必备章和破碎标题 |
| A05 | 参数／评分／格式表混合 | 用途正确，不全当投标格式 |
| A06 | 截短投递、工具失败、输出截断 | 不虚记覆盖、不越过未检查范围 |
| A07 | 归纳标题无字面命中 | 有依据章节仍保留 |
| A08 | 跨页／跨卷／补遗／冲突 | 影响结构／格式的缺口阻断，内容提示允许随文件发布；冲突不伪造解决 |
| A09 | 删除早期聊天历史 | 可正确恢复节点、父子和顺序 |
| A10 | 自引用／祖先环／批量合并删除 | 非法树原子拒绝，编译不卡死 |
| A11 | 格式未找到／条件未知／不适用 | 分别处理；预览与 Word 一致 |
| A12 | 预约／响应／提交／发布边界崩溃 | 不重复调用、建章、发布 |
| A13 | 跨旧时间边界、多次重试 | Worker／Rust／SQL 同一截止；到帽无编译；累计花费不重置 |
| A14 | DATE／REF 混合段落表格 | 普通域正文完整回读；本期不支持语义的域提交前拒绝 |
| A15 | 单章改名保留书签，重绑失败 | 不使用旧依据 |
| A16 | 图片签章、自定义样式、章外内容 | 图片／签章载体／样式／页眉页脚／非 TOC 章外内容全部拒绝，返回位置，原版本不变 |
| A17 | 填充期间保存新版本 | 拒绝覆盖 |
| A18 | 填充成功立即下载 | 新 current 身份 |
| A19 | 实际保存关闭重开、PDF 转换 | 内容保持，同版 DOCX／PDF |
| A20 | 旧模式、入口和文档 | 无旧分支、别名、双方案和断链 |

真实文件使用 `testdata/bid` 副本和已有冻结样本，保留原件摘要。人工清单标注必需章节、原文位置、格式附件、技术结构、条件与补遗影响；答案只用于验收，不发送模型或写入硬编码。

通过标准：必需项全部有正确去向，无无依据章，唯一发布阻断清单为空，所有非阻断提示在界面和 Word 可见；预览／Word／版本一致；用户内容无静默损失；数据库故障恢复和真实 ONLYOFFICE 链通过。记录输入／合同／输出摘要、模型配置、调用与读取量、耗时和问题。单测、数据库、真实模型与编辑器结果分别报告。

当前验证事实：此前旧大纲测试通过不证明新流程通过。T4–T8 离线证据（2026-09-18）：`bidding` lib 过滤测 150 passed／1 ignored；`bidding_v2_baseline_contract` 19 passed；web authoring 54 passed。已去掉 export 对 draft analysis 与未完成独立复核的发布硬门；同版 DOCX／PDF 绑定与 CAS／pending-save 门保留。BiddingFile.pdf（sha256 `4c80edd6…58fdc`，106 页／148 来源／42 表）离线 A02 绑定与 T1 分块校准（20 窗／204 块）已通过：`cargo test -p bidding --lib biddingfile_`。完整 A01–A16 与真实 PG／模型／ONLYOFFICE 链仍缺，T9 未关闭。

### 本轮评审补充验收

- A08 增加三例：外部标准内容引用未解但章节齐全可发布并显式提示；资格附件缺失导致子项未知不能发布；报价格式无依据不能发布。
- A09 增加核对工具越界测试：扫描式 source_index/search 和核对包外读取被拒；分册修改只失效相关包，顶层关系修改使顶层包失效；同签名两轮及 A→B→A 不续预算。
- A13 同一故障注入验证 Worker、Rust 和 SQL 返回相同绝对截止；旧 5／6 回合测试替换，保护额度不是质量断言。
- A16 明确本期白名单及包级拒绝：首章前内容不得丢弃，自定义样式不得放行；TOC 与系统待确认说明不使空章误判有用户正文。
- A20 同时检查工作树文件、全仓当前 Markdown 引用及交付差异；工作树删除而索引未暂存不等于已提交删除，不擅自改用户暂存区。


### 2026-09-18 Review 修复记录

本轮修复：要求／引用／问题分页保留 ID；核对片段包含冻结原文或网格文字；核对失败后保留定向发现阶段；发布门读取持久化阻断问题，未解决结构／格式引用不能因目标列表非空而放行；指定格式依据丢失阻断。核对快照包含引用，重新组织保留仍有效的其他包结论。非阻断问题保留在 Word 和大纲界面，原样系统提示不算待填章正文，用户编辑后按正文保护。

正文、表格（首次含表头）、空来源处置及 documents／document_relations／decisions 都进入分块及扫描记账；空文本且无网格的来源需要原始视图，不能仅凭空读取宣称已检查。当前样本计入元数据和空来源后为 204 块；不是性能验收。冻结 budget 记录 chunk_count、total_timeout_secs、publish_reserve_secs，调用／读取额度仍以同一 config.limits 为准，不复制第二套计数；领取、Worker 和 Rust 使用同一冻结期限，首次大纲预留 300 秒发布。

填充白名单修复 TOC 误拒绝、表格内样式漏检，并保留共享解析器的页眉页脚提取能力；填充侧按部件拒绝。系统封面／目录文字使用摘要书签，只有原样文字可跳过，编辑后的首章前文字仍拒绝。已移除新请求的 mode 字段和 Official 运行分支，不提供旧请求兼容。

本轮没有运行真实数据库、模型或 ONLYOFFICE 联合验收；本机未配置 KB_TENDER_AGENT_TEST_DATABASE_URL。该次验证尚未覆盖 T8 的最终清理；最新状态见下方 T8 清理记录。

本轮最终离线验证：`cargo test -p bidding --lib` 在允许本地端口的环境中 380 passed／17 ignored；SQL 基线静态合同 20 passed；`cargo check -p api -p worker` 通过；前端 54 passed 与 `tsc --noEmit` 通过。`tender_analysis_postgres --no-run` 已编译，但未执行数据库测试。移除的是旧 Official runtime 集成场景及其专用对象／模型模拟，保留其他 Journal、CAS、编译器测试；不得把静态 SQL 测试或仅编译当成数据库恢复验收。

补充验证：DocReader output inventory 完整测试 13 passed（含本地 gRPC）；回读保护测试 12 passed。Worker 读取冻结期限所需 SQL 函数已补授权，并加入静态合同回归。


### 2026-09-18 后续修复验证补充

- T3：问题持久化增加必填 description；Word 与界面展示问题说明及原文／网格／视图位置。核对包保留既有 finding_ids，遗漏报告不能清除问题；相同依据与结论无进展时保留非阻断警告并通过该包。阻断问题仍禁止发布。扫描工具 schema 已补齐要求依据、问题、引用和核对片段的实际字段。
- T4：模型预算只覆盖 drive，编译使用剩余发布预留；预算从进入运行时即开始计时。零模型预算只能恢复已核对检查点，未核对状态直接失败。删除 Worker 废弃的固定 46 分钟参数／常量；首次冻结的绝对截止仍约束最终发布。
- T6：标准 TOC 放行，TOC 混入 DATE 仍拒绝；表格段落、字符及表格自定义样式均拒绝。包根级数字签名关系也进入拒绝清单。
- T8：删除旧正式编制前端 API、启动组件与 session 测试；独立报告下载保留并单独测试。删除部署中的旧编制 limits 环境变量。该次仍遗留历史 Rust 运行体，现已由下方 T8 清理完成。
- 本次验证：bidding lib 383 passed／17 ignored；SQL 静态合同 20 passed；DocReader inventory 17 passed；前端 44 passed（删除废弃 session 测试后计数减少），TypeScript 检查通过；API／Worker 编译检查通过；git diff --check 通过。新增零模型预算编译恢复、警告遗漏后收敛以及实际 schema 载荷测试。未运行真实数据库、模型和 ONLYOFFICE 联合验收，不能据此宣称大纲业务完整性已验收。


### 2026-09-18 T8 清理记录

- 删除独立编制 Agent、agent_scope／agent_work、工具 schema、内嵌提示词、运行配置及专属运行／故障注入测试；样例 CLI 删除 compose 模式。
- 删除独立编制 SQL checkpoint_get／checkpoint_put／reserve／publish／replay 函数、执行授权及 composition_checkpoint／composition_main／composition_review 阶段值。保留当前填充使用的来源冻结、请求身份、DOCX CAS、对象暂存、发布／重放，以及独立报告查询。
- 通用编译器、模板字段定位、表格、原文引用及回读往返测试保留。旧 Workspace 仅作为 tests/compiler_fixture.rs 中的编译测试构造器，以 cfg(test) 私有模块编译，不对生产或模型暴露。
- 删除依赖旧独立编制夹具导出的 transport 验收脚本；现有 full_flow 脚本改为使用大纲任务发布的 draft_docx，直接移交同版 Word 给 Office 探针，不另发编制请求，不要求旧编制报告。填充 HTTP 测试使用当前 KB_TENDER_AGENT_LIMITS。
- 验证：bidding／API／Worker 全目标（含 docx-http-contract-tests 特性）编译检查通过；bidding lib 328 passed／17 ignored；SQL 静态合同 21 passed；full_flow／Office 身份离线测试 11 passed（另 4 个 subtests）；git diff --check 通过。Rust 测试数量减少来自废弃独立编制运行测试的删除，不代表额外业务验收。
- T8 记为已实现待验证；本环境尚未执行真实数据库迁移、填充 HTTP、模型和 ONLYOFFICE 联验。旧 transport 夹具不再作为当前流程验收证据，T9 需按新流程准备可运行的数据库／HTTP 夹具。

### 2026-09-18 追加评审修复与验证

本节记录工作区实现，不代表已部署或真实业务验收，T1–T8 维持“已实现待验证”。

| 评审项 | 实现与边界 |
| --- | --- |
| 1、2 修补与增量组织 | Discover 中已完成定向发现且存在原检查包时允许组织修补；保留检查历史。批次只阻止破坏原有要求映射，全部适用要求映射在 finish 验收。 |
| 3、5 核对原文与续读 | read_outline_fragment 支持当前包片段 UTF-8 字节续读；收据复用 analysis.coverage.metadata，以 outline-check:包快照摘要:片段ID 为键。实际投递先进入 pending_coverage，后续响应确认；提交检查要求当前快照所有片段完整送达。 |
| 4 解析缺失 | documents 中 failed/pending/unresolved 阻断 scan_complete；读过失败元数据不等于读过文件。 |
| 6、7 回读保护 | 混合普通正文与 TOC 拒绝填充；拒绝未承载的直接格式、编号、修改标准样式、非 Normal 样式继承和表格内部格式。白名单检查增加表格边框及标题属性值校验。 |
| 8 问题延续 | 填充初始化同时带入原 outline 与回读章节，原问题和引用继续参与新 Word 合成；沿用冻结分析摘要。 |
| 9 批量树 | 新节点临时 ID 映射到宿主 chapter-N；先移除被替换和删除的节点，再检查最终树，支持顺序交换和同位置替换；拒绝同节点既更新又删除。 |
| 10 工具与期限 | 发现阶段开放有界 search_sources；填充写作时间取绝对截止剩余时间减发布预留，再与写作窗取最小值，恢复不重新获得完整时限。 |
| 80 轮故障 | 两个任务创建入口均在 REPEATABLE READ 事务中读取预算输入并调用 from_environment_for；避免漏传输入导致 chunk_count=0、冻结默认80轮。既有请求合同不改写。 |
| 调用效率 | 短来源合并为有预算的候选范围，实际发送仍受工具预算约束；不再提示每轮只检查一个块。204块不等于204次模型调用，保护额度不作性能目标。 |

本次验证：bidding lib 333 passed／17 ignored；SQL 静态合同 22 passed；DocReader inventory 25 passed；bidding/API/Worker all-target 编译通过。回归覆盖增量建章、修补、交换与替换、解析失败、长核对片段、填充警告、混合目录正文及格式保护。未执行新预算 SQL 的真实数据库集成、数据库故障恢复、真实模型耗时/完整性或 ONLYOFFICE 联验；静态合同不替代 SQL 执行，短来源候选合并不等于已证明耗时达标。

### 阅读包与覆盖分块解耦补充验证

发现游标只保存首个未扫描记账块；模型请求根据冻结配置派生阅读包，不再在 preload 层用 8KB 截候选范围。发现阶段正文可跨多个 8KB 记账块，表格不再限每轮一个记账行片段；保留原工具实际序列化限额、UTF-8 续读、表头投递和未发送范围不确认的规则。填充阶段不受此次调度调整影响。新增阅读包不建立持久化领域状态，恢复由冻结输入、配置与已有覆盖收据重建。

本次 `cargo test -p bidding --lib`：334 passed／17 ignored。新增回归通过实际 request 构造与收件确认验证：两个各 9,000 字节来源在较大预算下一起进入请求；缩小输出预算会缩小阅读包；准备阶段不登记覆盖；Checkpoint 序列化恢复后投递内容一致；收到原文不等于扫描完成。完整请求仍受上下文限制。`git diff --check` 通过。未运行真实模型的百页吞吐及人工完整性核对，不能据此承诺正常轮数或 5–10 分钟目标已经达成。

### 完整上下文不足时的缩包回归

新增后续轮次回归保留一条不可淘汰的大响应，使初始阅读包超过完整请求字节额度。断言最终请求仍含缩小的原文包、整体在预算内；准备阶段不登记覆盖；Checkpoint 恢复后能确认相同请求且仅记录缩小后的实际范围。另验证连最小包也放不下时显式失败、不虚增覆盖。没有以首轮空上下文测试替代此边界。

本次完整 Rust 回归：335 passed／17 ignored；`git diff --check` 通过。该测试为构造的拥挤上下文，不是失败请求真实冻结额度重放；当前 PostgreSQL 服务未运行，真实请求后续轮次及模型耗时仍待数据库环境恢复后验证。未部署。

### 剩余容量、核对证据及填充边界修复

- 阅读包先保留既有配置上限，再对包含历史、工具、状态和新原文的完整请求分别检查字节上限与输入估算 token 加预留输出的上限；先清理可淘汰历史，仍超限则本地减半重建并再次校验。禁止把剩余 token 直接当字节扣减。token 估算复用统一准入函数。新增回归单独制造字节未超限而 token 超限，并验证最终额度和恢复收据。
- 历史整理先于缩包失败：独有旧原文仅在已投递正文/网格/集合范围均有持久化扫描结论时允许淘汰，最新协议组保留；业务状态及收据不删除。不增加摘要 Agent。续读候选按剩余正文、网格比例与定位开销估算，不再以全文长度占位。
- 核对包由保存的全部要求与章依据/格式依据补齐强制原文片段，使用 basis-加跨度摘要标识并按分册关联；发现阶段保存的额外查漏片段仍保留。空包不能提交通过，每一条生成片段仍必须按当前包摘要实际送达确认。此机制不替代人工验证发现阶段是否漏提要求。
- DocReader 比较 docDefaults 与当前填充编译器固定的 SimSun 12pt、1.5倍行距默认格式，不一致时报告 /word/styles.xml 并拒绝填充。测试样稿采用产品支持默认值；另有30磅默认字号拒绝回归。任意外部默认格式不再被默认为可重建。
- 新填充立即派发首个空章；无空章时 API 在回读后重新核对 current 身份，返回 HTTP 200 的 `{status:"unchanged",current:{version_id,docx_sha256}}`，不创建任务/版本；前端显示没有待填空章。无操作无副作用，同次请求重试仍重核对版本。已启动任务零写作时间或没有新增模板内容时明确结束为未完成，保留旧版本，不调用无读取依据的编译兜底。

当前离线验证：Rust库339 passed／17 ignored；DocReader inventory26 passed；前端45 passed及 TypeScript检查通过；API/Worker all-target检查通过。SQL静态合同22 passed；最后续读估算调整后招标分析154 passed／14 ignored。数据库真实故障恢复、原失败请求真实额度重放、模型完整性/耗时、ONLYOFFICE联合验收仍待完成；未部署。

### 真实发现循环修复（请求 23d12a9e）

现场只读保存第30步检查点、预留请求及冻结输入/配置到本地临时目录，未修改运行任务。第22至35步维持46项要求、30个正文扫描来源和游标35；同一36单元格表格交替重复读取。预留请求证明整表读取曾因本批结果预算被拒，后续状态只给总进度，没有待提交范围；大纲路径每轮又把 Recovery 重置为 Running、replans清零。任务后来自行推进，不能将本次现象描述成永久死锁。

最小修复：
- 每轮 Discover 投影 pending_scan，精确计算已送达减已提交扫描的正文/网格/集合范围；read_outline(kind=pending_scan)支持分页。明确允许分段提交，也允许无新增要求的扫描结论；原文不在上下文时定向重读，跨来源补读继续开放。
- 发现阶段历史证据识别覆盖实际跨来源读取，不因宿主单来源游标而将其他表格证据视为无关、重复淘汰；沿用现有预算和安全历史清理。
- 发现进展复用现有 Progress：新增覆盖是进展，新增扫描范围是完成进展（要求、引用、问题和片段更新不单独清零停滞）；重复读取同范围不产生新版本，不清零纠偏次数。将 watch 投影给模型，纠偏额度耗尽后阻止新的调用并保留检查点，不自动发布。

离线现场回归使用原冻结额度131072 token、8192输出token，实际构造请求通过准入；待提交清单包含该表[0,36)，显式提交检查结果后游标推进，未自动宣告核对或发布。此为宿主回放加人工构造工具提交，不是证明真实模型一定会自主提交。测试默认忽略，使用 KB_DISCOVERY_LOOP_DIR 指向本地现场目录运行。另有普通回归验证重复读取耗尽纠偏且不虚增扫描覆盖。未部署，也未重启或中止现有任务。

本次验证：完整 Rust 库340 passed／18 ignored；最终限定 pending_scan 只在 Discover 可读后，招标分析155 passed／15 ignored；SQL静态合同22 passed；真实循环归档回放1 passed（单独启用 ignored 测试）；git diff --check通过。以上均不代表新实现已部署或真实模型循环已通过在线复测。


### 2026-09-18 发现吞吐与游标118修复

- 发现阶段热状态不再附带完整已保存要求正文，仅保留要求数量、扫描状态、待提交范围和最早未完成的 `cursor_chunk`。完整要求仍可通过 `read_outline` 分页读取，组织及核对阶段保留原有投影。固定系统提示和工具定义不变，不承诺缓存命中率。
- `read_source`、`read_form`、`collection_index` 仅在所请求结果的完整范围仍存在于本次已送达请求中时返回短收据。历史累计 coverage 不能代替可见性；同一响应中新产生的工具结果不能当成已送达；部分重叠不能吞掉未读范围。短收据不是原文，也不能产生扫描结论。
- 自动发现组包以“已扫描范围＋当前可见原文范围”选择下一段，累计已读仍保留作证据账本。已送达但未提交扫描且已从上下文淘汰的正文、表格、元数据会重新投递，避免游标永久落后于实际阅读。扫描结论仍必须由模型显式提交。
- 现场检查点第95轮游标118对应表格 `7c726851-7e75-4055-8a15-14b69401ad45`：累计已读 `[0,63)`，扫描仅 `[0,50)`。后续来源扫描继续增加要求，但该表尾部缺口未闭合。另有正文 `[0,56)` 缺口，不能把它误称为游标118的当前块。
- 统一 token 估算不再把 JSON UTF-8 字节数直接作为 token：完整序列化请求按 ASCII 两字符/token、常用中日韩统一表意字两token/字、其他字符按UTF-8字节回退，另加既有安全余量及每图预留。此为比现场约三字节/token更保守的启发式，非 grok 精确 tokenizer，也不是任意输入的理论上界；继续记录供应商实际用量及估算超出情况。完整请求准入和输出预留仍有效。
- 验收包含实际归档游标118检查点：模拟已有工具结果送达边界，重新组包必须包含 `[50,63)`；确认送达不得推进扫描，显式提交该范围后游标才前进。该验证不是实际模型自动完成、数据库故障恢复或部署验收；30–40轮、10分钟仍需真实运行测量。

本次本地验证：bidding 库测试342通过、19忽略；第30轮和游标118两个归档回归分别显式运行通过。游标118重组请求131972字节、估算输入70614token、阅读包上限32768字节，8192输出预留后通过冻结131072token准入；这不是供应商真实用量。SQL静态合同22项通过，未部署、未改变运行中任务。

### 2026-09-18 本轮闭环修复合同（本地回归通过，未部署）

1. 发现进展只以新增扫描范围作为完成事件；要求修订不是扫描完成。相同描述、类型、适用条件、依据、格式依据和顺序约束的空ID提交复用已有要求身份。历史淘汰逐消息组检查完成范围，不要求全局已读均已扫描。超长单元格按UTF-8范围续读，全文确认后才取得整格凭据；沿用Coverage内部范围账本，不增加领域表。
2. 已有阻断问题不能修改code或删除影响关系绕过；解除须保持原问题身份并提供有效解决依据、通过关联的确定性检查。组织完成后归属发现片段，顶层仅负责组成／顺序及明确全局条款；未确定分册的片段显式处理，不能默默归入顶层。
3. 填充失败仅跳过本次正文生成，不删除章节；失败原因与是否适用分开。当前DOCX回读种子是保留内容和结构的可信输入，历史招标依据与本轮新增内容读取凭据分开，不能伪造本轮已读。无新增内容保持当前版本。
4. 版式白名单逐项匹配编译器可重建属性；页面仅接受编译器默认A4纵向及25mm边距；表格列宽和连续重复表头写入回读收据并按现有编译能力保留，其他不支持属性提交前拒绝，系统原始产物必须可回读。

本轮必测：30次完全重复提取不增加要求或无限重置停滞；无关待扫描范围不锁住已完成历史；60KB单格可续读；阻断不能降级；填充超时不删章；保留A章仅填写B章可编译；页面／表格不支持属性明确拒绝；核对片段按分册归属。上述是实施验收条件，不代表真实模型、数据库故障恢复或ONLYOFFICE业务验收已经通过。


### 2026-09-18 游标181：组织修补闭环

只读现场检查点为请求`b0ab5fcf-5d3e-42c8-9c5f-df17d8dbf25c`第53轮：业务阶段已经是Outline、111项要求和35章，但`outline_run`仍为Discover/181，旧Main工作继续自动投递空文本。不能把181解释为实际扫描尚未完成。多轮finish_outline仅返回两个错误码；实际包括未关闭的结构/格式引用以及已映射要求的精确格式依据未挂到章节。组织阶段又禁止保存引用和要求修正，导致反复修改章节无法消除阻断。

修复合同：
- 发现完成时同步业务/执行阶段、游标置于完整分块末尾并清理发现Main工作；组织/核对阶段不自动投递发现阅读包。
- `read_outline(kind=blockers)`分页返回准确requirement_id、reference_id、chapter_ids及缺失格式span；finish_outline给出有界首屏及续读入口，不能只返回错误码。
- 沿用`submit_outline_scan`，组织阶段仅允许更新已保存要求与引用的依据/结论，范围字段必须为空；不新增工具或服务、不自动标记引用解决。依据仍须实际读取确认，核对阶段仍禁止此写入。修改后使核对失效，再次finish_outline进入核对。
- 新增内部字段落点：`Checkpoint.analysis.fill_seed_chapters`为保存DOCX的章节身份摘要，SQL首次与冻结种子核对、后续禁止修改；`Coverage.metadata["form-cell:<form_id>:<offset>"]`为超长单元格UTF-8续读范围，全文确认才登记form_cells；表格布局保存在DocReader inventory条目的`table_layout`及`draft_plan[].preserved[]`的`widths_twips/header_rows`。这些均复用现有JSON，不增表。
- Checkpoint合同版本统一为4；不恢复旧合同任务，不增加检查点升级链。本地归档仅用于显式构造新合同算法回归，不改写线上运行。


本轮验证：bidding库352通过、20忽略；schema合同3通过；SQL静态合同23通过；DocParser库52通过；DocReader inventory30通过；API/Worker all-targets编译检查通过；游标181真实归档回归单独启用通过。组织修补测试同时验证未读依据不能解除引用、组织更新不改变扫描账、核对阶段不能绕过只读边界。`git diff --check`通过。本轮未部署、未重启容器、未修改运行任务；尚未做真实模型端到端、数据库故障恢复和ONLYOFFICE联验，因此不能承诺实际生成耗时或所有真实文件章节完整性。


## 11. 要求已发现后仍慢：深入分析与统一修复方案（待开始）

本节属于2026-09-18目标设计修订，不是已完成实现声明。保留发现→组织→核对→发布、单Agent及已有Checkpoint／Journal；不引入新Agent、队列、领域表、向量去重服务或通用规则引擎。本轮仅分析并更新主方案，不修改产品代码或正在运行的任务。

### 11.1 真实证据与根因

证据取自请求`2d79b687-a9a4-459a-a320-e20b286f7c6e`保存的检查点与工具历史，分析截点为2026-09-18 19:44 UTC、第53步。这不是对任务后来状态的推测。

- 发现阶段从约19:14至19:34，31轮后完成204块；组织阶段又接近10分钟、22轮。截止时138条发现结果、57章，尚未开始核对。全量发现机制已有推进，不能继续把组织慢归因于未读完文件。
- 57章中50章只关联一条要求、4章关联两条、3章无要求而为结构节点。17次put_outline_items累计提交125项节点数据（含失败批次及修改），参数约50924字节，其中grounds与format_refs约25260字节。这个比例说明大量输出是在重抄已有依据，并非新增结构判断；不是模型token的精确统计。
- 4次批量写入被“一个标题列出多个组成项”拒绝。校验按两个顿号或括号分组判断，能误拒正常综合标题，也不能真正识别同义重复。20项批次中一项触发就整批重写。写入原子性正确，语义字符串硬门错误。
- 组织阶段两次read_source被“host must assign an active source pack before reading”拒绝。check_read_scope先取main_work再判断大纲路径；上一轮正确清理旧发现main_work后暴露了这条不相容前置条件。批量组织后的定向补读仍未完全打通。
- 来源里的提交物、响应细节、全篇语言／单位规则、加密上传流程、评分权重与公式，都被放入同一required清单；宿主把它们全部当作待映射要求。于是出现“法定计量单位”“华电投标客户端结构化编制与上传”等伪章节。
- 同一技术附表G分别变成“技术附表G 技术条款偏差表”和“按第三卷编制技术附表G”。完全相等的记录去重不能解决目录、组成条款与格式页对同一提交物的不同描述。
- `requirement-7`记载履约保证函不适用，`requirement-117`却要求提交并已建章；联合体相关条目也同时存在not_applicable与conditional。需要结合前附表、具体条款及补遗核对适用性，不能按最后遇到的模板判定，也不能对全部同名条目套统一优先级。
- 真实kind包含technical、qualification、composition、pricing、prescribed_form、prescribed_format等自由字符串，但指定格式缺失的一条确定性判断仅识别kind==format。语义分类和宿主验收合同没有统一。

以下是继续读代码确认的核对／状态风险，不声称已经在本次任务产生最终错误文件：

1. 核对包的要求集合主要由已映射章节反推；不适用条目未必有核对归属，其派生依据在没有分册归属时也可能不进入任何包。即使某个更大片段偶然包含相关原文，也不等于宿主要求核对该排除结论。
2. submit_outline_scan无条件使全部核对失效，包括仅重交原结论；每个packet_snapshot又包含所有引用。因此其他分册引用变化也会触发重查。
3. 树变更的volume_touch取直接父节点，不是所在分册根；新建非根节点因旧父不存在也可误标全局变化。已自动分配的fragment.volume_ids只在为空时推导，移章后可能残留旧归属。单一包装根还可能把本应分册核对的文档变成一个大包。
4. 未知chapter- ID可被当作新节点；错误的更新目标可能变成重复章。结构ID、临时ID的合同须严格区分。
5. 新核对问题没有统一验证requirement_ids、chapter_ids、reference_ids存在并在当前包范围内；关联缺失时issue_signature使用filter_map可静默忽略，导致无法定向修补。
6. 组织进展签名包含章节ID及未规范化的向量。新增无用章节、重排映射数组也可能被登记为新的完成进展；不等于待完成义务减少。
7. 5.47天保护期限来自当前公式，需修改设计，不是继续补SQL才能得到短耗时。前端只有章节数、要求数、轮次，无法告诉用户还缺什么，也不能据此可靠估算结束时间。

### 11.2 最小数据修订：区分处理方式，不另建要求系统

继续使用`outline.requirements`保存发现条目；它是所有相关条款的账本，不再把条目总数解释为应生成章节数。把现有自由字符串kind改成以下四个严格值：

| kind | 原文例子 | 正确处理 | 完成要求 |
| --- | --- | --- | --- |
| submission | 提交附件1A投标函、售后服务方案、检测报告 | 建立／复用实际承载材料的章节；一份材料可覆盖多条来源要求 | 适用材料有响应节点承接，必要子材料在树中可找到 |
| content_constraint | 有效期180天、质保三年、★参数响应、报价含税 | 关联已有投标函、服务方案、参数响应表、报价章节；不逐条建章 | 有明确承接位置且核对其内容责任；不得全部挂到空泛分册根 |
| structure_constraint | 商务／技术／报价分册、附件顺序、全篇中文与计量单位 | 约束树、格式或后续编制；全篇规则进入顶层核对，局部规则进入对应分册 | 有核对归属和处置结论，不要求独立正文标题 |
| non_document | 上传截止／加密操作、纯评分权重或计分公式、无需提交的背景说明 | 保留原文与分类理由，在结果中供用户查看，不生成章节 | 排除理由与原文进入语义核对，不能仅凭一个枚举绕过覆盖 |

**按实际材料拆分，而非逐句拆分。** 原文要求独立提交、独立填写或使用独立格式的材料，分别保存submission。同一材料、相同适用条件和承载位置下的参数／承诺允许合并成简短content_constraint；承载位置、适用条件或核对责任不同才拆开。一段同时要求“代理人签字时附授权委托书”和“文件盖章”，必须分别保留授权材料与签章约束，不得整段分类后漏掉材料。提取描述保留条件、数值、例外和材料名称，完整原文由grounds承载；不为减少输出删掉这些业务信息。

具体字段变更仅在现有SubmissionNeed：

- `kind`改为四值枚举。
- `submission_name`：仅submission要求非空，表示实际材料名称，例如“附件1A 投标函”；其余类型为null。是模型有据归纳的材料名，不是宿主按字符串创建的身份。
- `classification_reason`：保存分类理由；non_document以及改变既有分类时必填。已有grounds仍是分类依据，理由不能代替原文。
- `format_required: bool`：是否原文明确指定须遵循的表单／格式，不能由表格存在或kind名称推断。发现阶段允许暂存true且format_grounds为空的未闭合条目，同时保留待追踪引用；finish_outline／发布门才要求依据完整有效，不能在第一批发现时就要求找全格式。读取到标题或签字落款不自动证明整个格式已经取得。
- description、applicability、condition、grounds、format_grounds、order_constraints保留。不得把“无需独立章节”写成not_applicable；真实不适用须有原文条件，核对仍检查它。

发现阶段直接按四类保存。组织开始时对已有条目做一次有范围的归类／适用性整理，重点比较相同材料、前附表与通用模板、明确补遗、跨卷引用。不增加第二次全文发现。条目可保留不同依据，不强求先把138条语义去重到某个数字；优先让同一材料映射同一节点，这是比建设独立去重系统更简单的做法。

批量整理沿用submit_outline_scan。组织阶段范围字段必须为空；允许修正结论，必要的新条目仅能引用已扫描且实际投递确认的范围。没有这类依据就定向补证据或登记问题，不能伪造扫描完成。格式／适用性冲突保留双方来源：有明确适用规则才修正，否则按既有阻断与警告合同处理。不适用、non_document都不得成为模型随意删除要求的出口。

**组织时修正混合条目。** 优先保留原要求ID并缩小到其中一个明确义务，其余义务作为新条目保存；这不需要删除或历史迁移。确须合并并移除旧条目时，submit_outline_scan增加批次内`requirement_replacements`（旧ID→非空替代ID集合），替代对象必须已存在或来自本批新建返回身份；禁止循环、未知ID和无替代删除。宿主在候选状态中一次更新章节、引用和问题的关联ID，重算有效依据并使受影响核对失效，再提交；失败整批回滚。替换关联只是身份维护，不证明新材料已正确组织：混合材料拆开后仍须按下面的独立节点规则调整章节并核对。原先已解决的问题若受替换影响，重新打开；不继承旧语义批准。不另建要求历史表，Journal保留历史。

### 11.3 组织工具减负：模型只做结构决策，宿主处理重复数据

put_outline_items中的每个节点输入仅保留：`id,parent,order,title,purpose,prescribed,requirement_ids`，以及批次remove_ids。新节点用tmp- ID，已有节点必须使用真实chapter- ID；未知正式ID、同批重复ID拒绝并定位具体项。

模型不再输出章节grounds／format_refs。宿主对批次最终树执行：

1. 校验引用的要求ID和用途；正文承载节点须有适用提交物／内容约束支撑。组织节点可以绑定结构要求或由有效子树支撑。没有对应要求的新增实质内容先保存有据要求，不直接造章。
2. 正文节点grounds等于其承接条目的grounds并集；format_refs等于其承接条目的format_grounds并集。结构节点继承自己的结构依据及子树必要依据。按精确Span去重排序，不按相似标题找来源。
3. 要求修订、合并映射、移章或删章时在同一事务边界重算受影响节点与祖先；不能保留已经撤销的旧格式依据。冻结结果中仍保存有效grounds／format_refs，供编译和填充消费，不新建第二套领域库存。
4. 校验最终树、已有映射保护及依据，再一次提交。遇到非法项返回item索引／ID、字段、原因；仍然整批原子拒绝，不偷偷跳过错误节点。

批量组织按实际分册进行，先明确材料树，再把内容约束和重复来源批量挂到已有章。能在当前输出预算内一次提交一册就一次提交；大册可以增量提交，不再强求单批覆盖全部要求。没有“必须57章”或“每138条建138章”的验收数字。

**独立材料必须在实际Word中可定位。** 先建立材料树，再挂内容约束，两步允许在同一批调用中完成。原文规定分别填写的投标函、授权委托书、廉洁承诺可以共用父标题，但分别有响应子节点，不能仅以父节点关联三个要求ID验收。纯group节点不算提交物或内容约束的实际承载位置；原文明确的分册结构仍保留。一个参数响应表承接几十条参数可以合理，不设置每章要求数量帽。宿主验证用途、身份、映射和精确依据，独立材料是否错误合并由对应原文的语义核对验收，不能按标题相似度自动决定。

典型效果：两条技术附表G要求共享一个材料节点；报价含税和服务费约束挂到报价章节；投标有效期挂投标函；原厂质保、响应时限、服务内容挂服务方案。商务和技术偏差表与技术附表G若原文确实要求不同表单，仍分别保留，不靠相似名字强行合并。

移除concatenated_composition_title的标点硬拒绝。原文规定独立表单是否被错误合并，交给原文支持的提交物覆盖与语义核对；只改成另一套关键词黑名单不能解决问题。

### 11.4 补读、引用与核对必须形成闭环

**补读。** check_read_scope先区分业务阶段：Discover／Outline允许对当前冻结集合里的合法source／form ID定向读取，不要求已清理的main_work存在；Check仍只能读取当前核对包片段；Fill继续按填充作用域。自动投递与显式补读是不同职责，不为补读重新打开自动扫描。实际读到的范围仍走原有预约、确认和覆盖机制；同一响应中新读结果不能立刻当作已见依据使用。对应的历史可见性识别也须接受组织阶段的定向证据。

**引用。** 进入组织时一起处理未解结构／格式引用，不等到所有章节写完才碰finish_outline。引用需要关联影响要求，尚未识别要求时允许保留目标描述，但在核对组包前须确定负责范围。宿主可按冻结索引、定位和已保存来源提供候选；只有模型实际确认原文并保存resolution_grounds才算解决。不能看到目标ID就自动闭合，也不能把结构引用无依据改成content来绕过阻断。原文缺失时保留明确缺口；不无限重读。

**全部结论进入核对。** 检查对象包含提交物承接、内容约束归属、结构约束、non_document分类、不适用判定及冲突，而不只是已有章节。宿主生成“条目／排除结论→负责核对包”的派生映射：能由材料、依据和子树定位的进入分册；全局或尚无章节的排除结论进入顶层分类核对。每条至少有一个负责包；无归属不能宣布检查完成。不新增持久化关系表。

核对依据按权威要求类型和发现原片段派生：根节点为编译追溯继承的子树grounds不能全部算成顶层组成片段，否则顶层又会复查全文。核对单位沿实际章节子树选取；单一纯包装根不应吞并下面全部分册，保留其祖先上下文后按实际分支核对。大量材料仍按预算续读原文，不因包大而仅检查摘要。可将当前包的有界片段通过现有evidence_delivery自动投递，减少“先调用读取再提交”的机械回合；收据必须绑定实际投递范围和当前包摘要，不能因此放松原文确认。

**证据去重不等于免除核对责任。** 同包精确相同的Span只投递一次，并保留其关联要求／排除结论；重叠范围仅在同来源、同视图或同网格单元且可完整恢复各原范围时合并，不跨定位边界拼接。跨包共享片段仅因不同核对责任保留，不能全局读过一次就自动通过其他包。优先自动投递当前包尚未确认的有界原文，减少读取工具往返；按完整请求余量准入，分段收据绑定当前包摘要与实际区间。包改变后旧pass失效，不能仅凭发现阶段已读或旧包已读重新批准。

**防止漏抽取被核对继承。** 已保存要求的依据由宿主派生，模型不重复抄写为review_fragments；但发现时必须保留未形成要求、仍需检查的组成条款、格式目录、适用条件、补遗和明确跨卷引用片段。这些片段是对要求清单本身查漏的依据，不能仅从已提取要求推导全部核对输入。关键词只辅助定位，不作为独立验收标准。

**精确失效。** 在候选业务状态上重建包范围及摘要，比较变更前后结果，内容未变保留pass与收据，变了才标stale。摘要只包含该包相关引用，不包含无关全局引用；跨册引用进入所有受影响包，全局结构变化影响顶层及受影响分册。从最终树沿祖先找归属，不用直接parent假冒volume。自动推导的片段归属随树重算，显式指定归属单独作为现有volume_ids的输入语义保留并校验；不把推导值反写为永久人工指定。组织／扫描的空操作不能清空核对成果。仍复用现有packet_snapshot与checks，无需另做依赖图服务。

**问题身份。** 提交issues时一次校验全部关联ID存在、属于当前包或明确的跨范围发现，依据属于实际确认的原文。发现尚不存在的材料时以原文和目标描述报告，不伪造requirement_id。找不到关联对象不能被filter_map静默忽略。问题修补完成后只重查受影响包，非阻断警告保持已保存内容，不靠下一轮不再报告来消失。

### 11.5 Session、进展和预算

组织阶段每轮投影：当前分册、紧凑现有树、待承接条目、待核对的分类／适用性冲突、未解结构／格式引用、具体格式缺口。完整要求原文和依据按需分页；不要每轮从同一页重复投递已完成要求。没有确定分册时先投影所有材料名称与ID，再由模型组织顶层，不用关键词强行分册。大树分页仍需保留父链、同级顺序、next与总量，历史压缩不能成为唯一状态来源。

状态和依据长期保存在Checkpoint，聊天历史只是有界上下文。保留最新未处理工具组及当前核对必要片段；可重读且已提交业务结论的旧证据允许清理。先消除重复结构／来源输出，再判断是否还需要模型摘要压缩；本期不新增压缩Agent或把摘要作为引用凭据。固定系统提示与工具schema，缓存优化只看实际cached_tokens，不承诺命中率。

组织完成进展依据宿主派生的已承接条目、有效约束去向、已解决引用及已通过包。数组先按集合规范化；增加一个无用章节、重排ID、复制旧依据、只改标题措辞不能清零完成停滞。合法结构调整可作为普通进展，仍受现有focus/no_progress保护；不是每次写入成功就完成一个工作单元。

前端从同一Checkpoint派生显示：扫描块、有效提交物／内容约束已承接与未承接数量、结构约束待核对、分类／适用性冲突、阻断引用、格式缺口、已核对包／总包。不要把138条发现记录渲染成138个待建章任务。阻断摘要至少展示首项内容和依据位置，其余分页；不凭章节数给完成百分比或可靠ETA。

性能目标分开验收：先证明“已有要求→完成组织”不再几十轮反复复制。本次三分册样本以4–8个组织模型回合作为首次校准目标（含归类及批量映射，复杂冲突另列），不是硬帽。50.9KB节点调用参数中的来源复制应基本消除；比较同一文件真实输出token、失败调用、阶段P50/P95耗时与人工完整性清单。发现仍需约20分钟，说明只优化组织不能承诺整个流程10分钟；全流程5–10分钟保留为目标，在真实模型测量前不对用户给固定结束时间。分块数、模型回合数、章节数始终不是同一个量。

总时限采用第5节独立冻结预算，保留累计截止、重试不延期和300秒发布预留。不热改当前请求，不用扩大828轮或截断来源绕过性能问题。后续大文件保护值根据完整扫描量和模型实测校准，不能承诺任意文件都在同一个分钟数内完成。

**效率实施约束与测量。**

- 发现：每个有余量的请求自动投递新的或尚未确认的证据，附必要上下文／表头；已确认但尚未提交的范围优先提交结论。允许无新增要求的扫描批次，不强迫模型为了推进创造条目。跨块上下文不重复计入扫描完成。
- 输出：要求采用简短业务结论加精确依据；组织输出只写结构和要求ID；核对输出结论与实际问题。依据可宿主派生时不再要求模型复制UUID、字节范围和原文。不能硬截断条件、数值和问题描述来满足token指标。
- 工具：合法边界、已有ID和候选身份由工具返回，修正后仍严格验证；不静默更改错误范围，不将同一响应中新读内容算作已见证据。一次调用能完成一个有界批次就不拆成逐条回合。
- 上下文：发现轮只投影待提交范围、游标和必要关联；组织轮投影材料树、未承接条目和阻断项；核对轮投影当前责任包。完整账本按需分页，Checkpoint仍是真实状态。先减少重复输出和往返，本期不引入摘要Agent或额外并行审查。
- 度量：沿用现有日志／Journal离线统计阶段墙钟、逻辑回合、物理尝试、模型等待、输入／输出／缓存token、调用参数字节数、补读和失败调用、核对失效次数及片段重复量。不为性能指标新增领域表。多次同文件同配置运行才报告P50／P95，单次只报告实测，不把工具调用数当模型轮数。
- 验收：先满足人工材料及条件清单，再比较同文件基线。记录构建、模型、配置、冻结输入摘要，原失败请求用于离线复现，新的合同使用新请求实跑，不热改在途任务。组织4–8轮和全流程5–10分钟仍是校准目标；不得把减少必要章节、漏扫来源、漏查排除结论或跳过发布门当成优化成果。

本次样本基线单独记录：请求a441f3dd在第30步完成204块发现，约19分24秒；第44步进入核对，122条记录、13个节点、113条关联，组织约5分钟。6个核对包有296次片段引用、248个独立片段；这些计数不是完整性结论。第64步核对结束后因第11.8节收尾缺陷未发布，不能将1527秒记为成功交付耗时。

### 11.6 实施顺序、文件边界和验收

O1先解除真实阻塞，O2改变“已发现要求→组织章节”的主要工作量，O3防止核对再次放大耗时。O4/O6只在合同明确后跟进，O5贯穿；同一文件由单一实现者顺序集成，不把共享类型分别交给多方各写一套。

| 任务 | 主要文件 | 必须验证 |
| --- | --- | --- |
| O1 | agent/context.rs、draft.rs、tests/draft.rs | 实际after_batch切到Outline清理main_work后，经真实工具分派可读合法来源；Check拒绝自由读、未知源拒绝；综合标题不被标点误拒；未知chapter-ID不新建；坏批次不部分提交 |
| O2 | outline_flow.rs共享类型、draft.rs、两个outline schema、outline prompt、docx_composition编译消费 | 四类条目严格序列化；资料共章、细节挂章、规则不造章；格式布尔门正确；依据自动并集且更改后撤销旧依据；填充继承有效依据且不改用户正文 |
| O3 | outline_flow.rs组包／摘要／issues、agent/evidence_delivery相关边界 | 不适用／non_document有负责包并核对原文；修改技术册不重做无关商务册；移章、删章、跨册引用准确失效；原文未确认不能通过；未知问题ID原子拒绝 |
| O4 | agent/context.rs、agent.rs::progress、web/src/bid/api/types.ts、AnalysisProgress.tsx | 清除历史仍能批量映射；无操作／数组重排不刷完成；界面显示真实剩余条件，数字来自同一状态而非前端再算规则 |
| O5 | tests/draft.rs、outline_flow.rs tests、testdata/bid及隔离验收产物 | 原失败请求回放和真实模型独立验收；原文件保持不变；有人工材料／约束／不适用清单，覆盖末尾技术、长表、跨卷、补遗、恢复 |
| O6 | agent.rs::RunBudget、postgres.rs、Worker、SQL基线及静态合同 | 总截止不由回合最坏耗时相乘；重试不延期；剩余预留不开始模型；失败不发布半成品；模型完整性和耗时单独报告 |

交付分三批，不新增架构：第一批O2完成分类、混合义务拆分／替换、独立材料树和依据派生；第二批O3完成全部结论核对归属、证据去重及精确失效；第三批O4与O5完成阶段自动投递、紧凑状态、真实进度及同文件性能／人工完整性验收。O6预算保护贯穿，不用扩大额度替代提效。每批同步类型、schema、提示词与回归，不能只改提示词就声称合同已经实现。

新增必测：混合“授权材料＋签章规则”不漏材料；同一参数表不被拆成几十章；三份独立格式在Word中有独立节点；要求替换后无悬空引用且旧问题不自动解决；没有提取为要求的组成原片段仍进入核对；同包共用原文不重复投递但所有核对责任保留；局部修补不重做未变包。新增方案尚未实现，不改变第9节当前任务状态。

所有新字段、工具输入与摘要合同一次统一升级。更新schema、Rust、SQL冻结摘要及前端DTO；旧字段拒绝，不提供旧运行升级链，不修改线上已冻结任务。保留既有DOCX及用户文件。

本次额外验收样例：

1. 同一附表G来自目录和格式正文，只生成一个真实材料章，双方依据保留；附件4若为另一张表仍保留。
2. 有效期、含税、质保期限分别归入已有响应章，不生成同名零碎章。
3. 中文、单位、加密上传、评分公式都得到正确分类与核对，没有以not_applicable掩盖未知提交义务。
4. 前附表明确排除联合体／履约保证，后文通用格式仍存在：结果不得被最后出现的模板反转；适用性结论进入核对，无法确定时保留问题。
5. 标准格式名称不论模型用什么业务词描述，format_required缺依据均阻断；目录提到表名不等于拿到完整格式。
6. 20节点批次含合法综合标题一次成功；非法树返回具体项且无任何部分写入。临时ID解析与请求重放稳定。
7. 自动grounds／format_refs与人工预期Span集合一致；更换要求依据后旧Span不残留；编译仍验证实际投递凭据。
8. 不适用条目／non_document结论未核对时不能发布；读取一个无关片段不能代替这项核对义务。
9. 一个分册局部修补后其他有效包及其原文收据保留；共享引用变化只影响真正相关包；空操作不失效；根节点继承子树依据不导致顶层重新核对整份文档。
10. 组织模型输出中不再反复复制原始UUID／字节范围；要求数组顺序重排不会增加完成进度。实际模型批量组织结果由人工清单验收，不以宿主模拟通过替代。

本轮方案的边界：不承诺零语义错误、固定章节数或任意大文件10分钟完成；不改变“核对通过才发布”和可选填充两次运行。优化的是错误工作量和机械重复，同时补齐核对分母，不是减少应该检查的招标内容。

### 11.7 本轮实现记录（2026-09-18，未部署）

- O1：Discover／Outline定向补读先校验冻结身份，再按阶段放行，不依赖main_work；Check自由读取拒绝，Fill继续要求活动作用域。历史可见证据识别同步包含Outline。移除标点标题硬拒绝。批量新建要求tmp- ID，未知稳定ID拒绝，错误附节点位置且整批不提交；schema与提示词同步。
- O4局部：完成进展按已承接要求集合及其格式依据集合计量，排除Omitted；改章ID、改标题、重排数组不增加完成版本。普通结构编辑仍进入普通进展。四类条目的去向、分册投影、界面剩余义务尚待O2／O3后完成。
- O6局部：新请求的runtime.budget.total_timeout_secs独立冻结为3600，publish_reserve_secs仍为300；不再按回合帽和provider timeout乘出数天期限。现有Worker／SQL消费冻结期限的链路复用；真实数据库恢复及保护值校准尚未验收。
- 本地验证：`cargo test -p bidding --lib --test bidding_v2_baseline_contract --test authoring_schema_contracts`，退出0；356单测通过、20忽略，schema合同3通过、SQL静态合同23通过。包括真实execute_turn组织补读、Check／Fill隔离、未知章节ID原子拒绝、综合标题、进展集合和独立期限回归。需要本地端口的测试在允许本地监听的环境重跑通过。
- 编译验证：`cargo check -p api -p worker --all-targets`退出0。
- 未完成：O2四类条目和宿主派生依据、O3核对归属与精确失效、O4其余项、O5真实模型／人工完整性验收、O6参数校准。上述本地测试不能证明端到端耗时已达标；未重新部署或改写在途任务。

### 11.8 编译收尾检查点修复（2026-09-18，未部署）

实测请求`a441f3dd-b84f-4a9b-8929-e4fa0522b808`在64轮、约1527秒后，六个核对包均pass，最后已保存sequence=192、done=true、outline.phase=complete，但review=null且没有编译对象。finish_draft_path在同序号追加review与DOCX导致divergent checkpoint，又吞掉该错误继续发布，SQL因结果与持久化检查点不同拒绝。此失败与回合帽无关。

修复合同升级为checkpoint v5（传输适配器仍为rig-chat-0.42.0/4，不做旧任务升级）：

- 编译收尾是无待处理模型回合的独立Journal序号；不增加模型轮次，不改变analysis、覆盖、计费或session。
- SQL仅允许受限的编译字段和确定性draft报告变化，检查DOCX对象哈希；首次大纲必须已有完整核对结果。相同序号仍只允许完全相同内容重放，不能覆盖。
- 收尾保存失败立即返回，禁止内存结果继续发布；对象暂存也不再用重新编译掩盖缺失的持久化结果。
- 已有编译结果恢复时复用；保存确认丢失后不再请求模型、不再编译、不重复追加收尾序号。
- 日志区分draft_compile_ready与数据库成功后的draft_publication_committed。队列success=true仅表示处理函数正常返回，也可能返回status=failed；不能作为业务发布成功证据。

验证：bidding库359通过、20忽略，schema合同3通过，SQL静态合同24通过；API／Worker全目标编译通过。另对实际失败检查点在数据库只读事务中执行新收尾校验表达式，正常结果通过，变更analysis、turn、review、DOCX哈希及session的五个反例均拒绝。该只读验证不是完整checkpoint_put写入／发布／故障恢复联验；未修改线上SQL、重启容器或重新提交失败任务。部署时Rust与SQL合同必须同步，旧冻结请求不自动转换为v5。

### 11.9 材料合同第一批实现（2026-09-18，未部署）

- SubmissionNeed改为四类枚举，保存submission_name、classification_reason、format_required；分类变更／non_document要求理由。发布门以用途及适用性判断实际承载，group不能代替响应材料；格式标记不再依赖自由文本kind="format"。
- put_outline_items只接收结构及requirement_ids，旧grounds／format_refs输入拒绝。宿主按最终树由叶至根合并、排序去重依据，更新要求后重算并撤销旧Span，编译／填充仍消费原有章节依据字段。树遍历使用子节点索引和待处理计数，不反复扫描全树寻找下一层。
- 组织允许在已扫描且确认的依据上新增拆分条目。requirement_replacements支持已有ID或本批tmp-目标，拒绝空替代、未知ID和替换链／环；整批更新章节、引用、问题关联。受影响的引用和问题重新打开，旧原文保留为核对片段，失败不改变检查点。
- 不需要章节或不适用且没有章节的结论进入顶层包的要求投影与原文依据；分组根继承的子树依据不再直接全部提升为顶层组成片段。核对包精确失效、跨包职责收敛与自动投递仍属O3，尚未完成，不能声称已经解决所有重复核对。
- 本批严格合同为checkpoint v6，Rust／SQL同步；第11.8节v5是前一批历史记录。无旧冻结任务升级或线上状态改写。
- 效率离线对照：从a441f3dd归档提取4次put_outline_items、20个节点条目，对同一数据作紧凑JSON序列化，去掉依据字段前20046字节、后7091字节，减少64.6%。这里只证明参数体积减少；未调用真实模型验证token、墙钟或材料语义完整性，不能将该比例当作耗时降幅。
- 验收边界：本地回归覆盖依据派生及撤销、分组不能承接材料、格式布尔门、非文档核对、组织有据拆分、旧类别拒绝、原子替换和旧结论重开。独立材料是否误合并仍须真实模型对照人工材料清单核验，不以所有requirement_ids均映射替代。
- 本批本地验证：`cargo test -p bidding --lib --test authoring_schema_contracts --test bidding_v2_baseline_contract`退出0，364单测通过、20忽略，schema合同4通过、SQL静态合同24通过；`cargo check -p api -p worker --all-targets`退出0。未执行新合同的真实模型、数据库发布或编辑器联验，未部署。

### 11.10 扫描批次定向修正（2026-09-18，未部署）

实测归档请求 `e7e8e1e4-a677-4c09-9391-c27efd1b9571` 在第4轮仍为 cursor=0、要求=0，但已有142293读取字节。失败提交先后涉及结构约束误标独立格式、条件缺失、核对片段不接受临时ID；前两次模型输出均超过7300 token、耗时约93–95秒。读取收据不等于扫描结论，不能通过自动登记扫描完成掩盖失败。

本批合同为 checkpoint v7，Rust与SQL同步，不增加状态表或旧任务升级链：

- requirements、references、issues、review_fragments 统一接受空ID或 `tmp-` 别名新建，已有稳定ID更新，返回按类别分组的 `id_map`。宿主映射批内关联。
- 提交前聚合可独立检查的字段、身份、关联和依据错误，返回路径、错误代码和总数；反馈受工具预算约束，截断明确标记。互相依赖的整体状态校验仍由最终提交执行，不能保证一次枚举所有语义问题。
- `submit_outline_scan` 支持互斥的完整提交或 `repair {call_id, arguments_sha256, changes:[{path,value}]}`。修正只替换扫描参数中的字段，再执行完整证据与状态校验。未通过时不提交任何扫描范围、要求或身份分配。
- 修正基线复用现有Journal内失败工具参数；绑定最新失败调用及参数摘要。失败修正可继续修正，成功扫描使旧基线失效。历史清理保留重建当前失败批次所需的完整工具组，成功后解除保护。没有新增摘要Agent或额外模型回合。
- 界面显示实际扫描游标／分块数；存在失败批次时显示“正在修正扫描提交”。读取字节不用于宣称扫描推进。

验证覆盖：统一别名与关联、聚合错误、仅修改字段后成功、错误身份／摘要／越界路径拒绝、检查点序列化恢复、历史清理保留失败链、成功后旧修正失效，以及真实execute_turn路径的结构化反馈和进度投影。完整本地回归为 bidding 库369通过、20忽略，schema合同5通过、SQL静态合同24通过；前端45通过。API／Worker全目标编译与前端类型检查通过；另新增1024字节错误反馈边界回归1项通过。

此修复减少校验失败后的整批重复输出，不改变来源全量发现与材料完整性门禁。没有部署、改写线上任务或验证新合同的真实模型耗时；不能据此承诺当前运行任务自动恢复，也不能把定向修复视为O3核对效率工作全部完成。

### 11.11 组织阶段批次修正与紧凑清单（2026-09-18，未部署）

真实任务 `e7e8e1e4-a677-4c09-9391-c27efd1b9571` 在第37轮完成204块扫描、保存170项要求；第41轮已有4章，直到第64轮才增至17章，期间多次因要求与章节用途不匹配而整批失败。材料全部映射与章节数量都不能替代材料完整性验收。

本批实现：

- `put_outline_items` 聚合可独立检查的身份、用途和要求去向错误，返回JSON指针、代码和处理方向。复杂最终树校验仍执行原有完整校验，可能返回整体错误；不宣称一次穷尽所有相互依赖问题。
- 复用扫描批次的Journal修正机制，新增互斥的章节 `repair {call_id,arguments_sha256,changes}`；绑定该工具最新失败批次，不允许修改扫描字段或检查点。失败批次相关工具组在历史淘汰时保留。所有修正仍原子校验最终树，禁止自动删关联或部分提交。
- `read_outline(kind=organization)` 提供分页紧凑清单：ID、类别、材料名、描述、条件、排除理由、格式标记、合法去向及已有章节；未承接响应要求优先。组织热上下文使用同一投影，完整依据仍通过 requirements／原文读取工具获取。
- 混合材料由模型对照已检查原文拆分，复用 `requirement_replacements`；适用性冲突依据提交阶段、条件和补遗处理。不得仅按名称自动合并，不得把“中标后提交保函”与“投标时承诺提供保函”视为同一义务。组织提示明确要求先处理这些问题，不引入关键词强制拆章或新Agent。
- 扫描结论修正、章节批次写入成功后，以最终候选重建核对包并比较原有通过摘要；完全一致才复用通过结果，变化或无法计算摘要时重新核对。问题记录保留。现有摘要仍包含全局引用，引用修改可能保守地使多个包失效；本批没有声称完成O3全部跨包范围优化。
- 合同升级checkpoint v8，Rust和SQL同步，不升级旧冻结请求，不修改线上任务。

验收范围：聚合多个用途错误、恢复后局部修正、失败不写入、排除结论保留、组织分页合同、互斥修正schema，以及最终候选不变时保留核对通过／变化时失效。本地完整回归：bidding库372通过、20忽略，schema合同6通过，SQL静态合同24通过；API／Worker全目标编译通过。仍需新任务对照人工材料清单核验独立材料、适用条件、补遗及实际模型耗时；提示改进不等同于已经证明混合材料和排除结论的语义正确性。
